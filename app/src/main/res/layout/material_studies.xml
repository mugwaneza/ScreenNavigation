<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:background="@color/amber"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/editTextTextPersonName2"
        android:layout_marginTop="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableLeft="@drawable/round_account_box_green_a100_24dp"
        android:ems="10"
        android:inputType="textPersonName"
        android:hint="Type your full name"
    />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/floatingActionButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/design_default_color_error"
        android:foregroundTint="@color/design_default_color_secondary"
        android:outlineSpotShadowColor="@color/design_default_color_secondary"
        android:clickable="true"
        app:srcCompat="@drawable/baseline_add_alert_red_400_24dp" />

    <TextView
        android:id="@+id/textView12"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12dp"
        android:textColor="@color/pink"
        android:layout_margin="10dp"
        android:text="Support Android Studio Multi-Module Architecture, You can select and add icons to particular module.
Added Support for multiple icon themes(Filled, Outline, Rounded, Two-Tone, Sharp)
Icons downloaded at the runtime instead of pre-build with plugin to reduce plugin size" />
</LinearLayout>